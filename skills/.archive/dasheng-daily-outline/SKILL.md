---
name: dasheng-daily-outline
description: Use only when replaying a historical outline-only workflow; the current mainline no longer exposes outline as a standalone stage and routes directly from brief to draft under dasheng-media-sop.
---

# dasheng-daily-outline

## 状态

`historical module / 已退出主链`

## 当前定位

旧版流程里曾单独存在 `outline` 阶段。

当前正式主链已改为：

`brief -> draft`

大纲仍可作为 Draft 内部准备动作存在，但不再是单独对外阶段，也不再作为独立 skill 暴露。

## 当前标准入口

- 总控：`../dasheng-media-sop/SKILL.md`
- 阶段定义：`../../引擎/03_全链路SOP工作流/STAGE_INTERFACES.md`

## 保留用途

- 回放历史产物
- 调试旧版 Lemon DNA 大纲逻辑
- 对照旧版文档命名

默认不要再直接调用本 skill。
